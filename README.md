# 楊曉熙 Lucy Yang — Personal Site

Source for https://yalucy1103.github.io/ — Next.js static export (App Router + Tailwind v4).

Build: `npm install && npm run build`, then push the `out/` contents to the `gh-pages` branch.
